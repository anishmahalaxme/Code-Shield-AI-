# Backend environment (copy to .env — .env is gitignored)
# Get a key at https://console.groq.com/keys

GROQ_API_KEY=your_groq_api_key_here

# Optional fallback name still supported by the code:
# GEMINI_API_KEY=...
